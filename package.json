{
  "name": "remindr",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted reminder app — no database, JSON storage",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,yellow \"npm run dev -w client\" \"tsx server/src/index.ts\"",
    "server": "tsx server/src/index.ts",
    "build": "npm run build -w client && npm run build -w server",
    "lint": "npm run lint -w client && npm run lint -w server",
    "test": "npm run test -w client && npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^8.0.14"
  }
}
